# Spaceship readme

This readme has a list of all the current symlinks that should be made
It also attempts to list some important to install software

## important software

i3
rofi
dunst
pywal
wal_steam

## symlinks
config/* > ~/.config/*
etc/preferences > /etc/apt/preferences
etc/sources.list > /etc/apt/sources.list
nanorc > ~/.nanorc
stamps > ~/stamps
userChrome.css > ~/.mozilla/firefox/profile.default/chrome/userChrome.css
vimrc > ~/.vimrc
wal > ~/.cache/wal
xinitrc > ~/.xinitrc
Xresources > ~/.Xresources
zshrc > ~/.zshrc
