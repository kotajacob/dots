#!/bin/sh

# rename the files based on their date
exiftool "-FileName<CreateDate" -d "%Y%m%d_%H%M%S.%%e" "$1"

# copy pictures info folders organized by date
exiftool -o . '-Directory<FileModifyDate' '-Directory<DateTimeOriginal' -d %Y/%m "$1"
