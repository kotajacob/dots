#!/bin/bash

selection=$(fd -e desktop . /usr/share/applications /var/lib/flatpak/exports/share/applications | fzf)
if [[ -n $selection ]]
then
	name=$(basename "$selection")
	cp "$selection" "$HOME"/.config/dprint/
	nvim "$HOME/.config/dprint/$name"
fi
