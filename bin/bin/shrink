#!/bin/sh
for source in "$@"
do
	base=$(basename -- "$source")
	ext="${base##*.}"
	name="${source%.*}"

    magick "$source" -scale "3000x3000>" "$name"_small."$ext"
	jpegoptim -m 92 "$name"_small."$ext"
done
