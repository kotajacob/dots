#!/bin/sh

NAME=$(echo "$HOME" | sed 's/\/home\///')

SEARCH=$(fd . --type f "/home/$NAME/docs/books/" -e pdf |\
	sed "s/\/home\/$NAME\/docs\/books\///" |\
	sed 's/).*/)/' |\
	dmenu -i -l 8 "$@")

SELECTION=$(fd . --type f "/home/$NAME/docs/books/$SEARCH" -e pdf)
zathura "$SELECTION"
