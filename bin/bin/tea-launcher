#!/bin/sh

COMMAND="head -n 1"
TMP=$(mktemp)

foot -o app-id="foot-float" -W "12x1" bash -c "$COMMAND > $TMP"

SELECTION=$(cat "$TMP")
foot -o app-id="foot-float" -W "12x1" tea "$SELECTION"
rm "$TMP"
